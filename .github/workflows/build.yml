name: Build APK

on:
  push:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Build with Buildozer Docker (stable)
      run: |
        docker run --rm -v ${{ github.workspace }}:/home/user/hostcwd \
        kivy/buildozer:stable android debug

    - name: Upload APK
      uses: actions/upload-artifact@v4
      with:
        name: counterapp-apk
        path: bin/*.apk
